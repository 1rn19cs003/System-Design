# Caching — Diagrams

- `assets/hld-caching/cache-hit-miss.svg` — the hit path (return cached value immediately) vs. the miss path (fetch from the slow source, store it, then reply)
- `assets/hld-caching/lru-eviction.svg` — a full 3-entry cache evicting its least-recently-used entry to make room for a new key

See them in context at: `pages/hld/caching.html`
