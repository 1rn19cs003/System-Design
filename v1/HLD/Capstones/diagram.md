# Capstone: Rate Limiter — Diagrams

- `assets/hld-capstones/token-bucket.svg` — a bucket refilling continuously, a request spending a token when one's available, and rejection when the bucket is empty
- `assets/hld-capstones/fixed-window-vs-token-bucket.svg` — a fixed window counter allowing nearly double the intended rate at its reset boundary, vs. a token bucket that stays bounded by capacity at all times

See them in context at: `pages/hld/capstones.html`
