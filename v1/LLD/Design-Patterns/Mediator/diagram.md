# Mediator — Diagrams

Rendered SVG diagram used on the website page:
- `assets/mediator/class-diagram.svg` — ChatRoom (Mediator) routing messages between registered User (Colleague) objects, none of which hold references to each other

See it in context at: `pages/lld/design-patterns/behavioral/mediator.html`
