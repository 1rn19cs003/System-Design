# Chain of Responsibility — Diagrams

Rendered SVG diagram used on the website page:
- `assets/chain-of-responsibility/class-diagram.svg` — TeamLead → Manager → Director chain, each holding a `next` reference, an expense request passed along until a handler's threshold covers it

See it in context at: `pages/lld/design-patterns/behavioral/chain-of-responsibility.html`
