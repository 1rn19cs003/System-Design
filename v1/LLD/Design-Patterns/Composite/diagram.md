# Composite — Diagrams

Rendered SVG diagram used on the website page:
- `assets/composite/class-diagram.svg` — FileSystemComponent interface implemented by both File (leaf) and Folder (composite, holding a list of children)

See it in context at: `pages/lld/design-patterns/structural/composite.html`
