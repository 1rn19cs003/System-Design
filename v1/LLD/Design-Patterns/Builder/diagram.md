# Builder — Diagrams

Rendered SVG diagram used on the website page:
- `assets/builder/class-diagram.svg` — ComputerBuilder with chainable setter methods and a build() step producing an immutable Computer

See it in context at: `pages/lld/design-patterns/creational/builder.html`
