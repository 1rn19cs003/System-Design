# Adapter — Diagrams

Rendered SVG diagram used on the website page:
- `assets/adapter/class-diagram.svg` — PaymentProcessor target interface, ThirdPartyPaymentAdapter implementing it while wrapping the incompatible ThirdPartySDK

See it in context at: `pages/lld/design-patterns/structural/adapter.html`
