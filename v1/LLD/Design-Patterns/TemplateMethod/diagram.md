# Template Method — Diagrams

Rendered SVG diagram used on the website page:
- `assets/template-method/class-diagram.svg` — CaffeineBeverage base class defining prepareRecipe() as a fixed sequence, with Tea and Coffee subclasses implementing only the brew() and addCondiments() steps

See it in context at: `pages/lld/design-patterns/behavioral/template-method.html`
