# Iterator — Diagrams

Rendered SVG diagram used on the website page:
- `assets/iterator/class-diagram.svg` — Playlist (Aggregate) creating a PlaylistIterator, exposing hasNext()/next() to traverse songs without exposing internal storage

See it in context at: `pages/lld/design-patterns/behavioral/iterator.html`
