# Command — Diagrams

Rendered SVG diagram used on the website page:
- `assets/command/class-diagram.svg` — RemoteControl (Invoker) holding Command references (LightOnCommand, LightOffCommand), each delegating to a Light (Receiver), with an undo history stack

See it in context at: `pages/lld/design-patterns/behavioral/command.html`
