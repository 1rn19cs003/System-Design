# Proxy — Diagrams

Rendered SVG diagram used on the website page:
- `assets/proxy/class-diagram.svg` — Image interface implemented by RealImage (expensive to load) and ProxyImage (lazily creates RealImage only on first display())

See it in context at: `pages/lld/design-patterns/structural/proxy.html`
