# Flyweight — Diagrams

Rendered SVG diagram used on the website page:
- `assets/flyweight/class-diagram.svg` — TreeTypeFactory caching shared TreeType (intrinsic: name, color, texture) flyweights, reused by many Tree instances (extrinsic: x, y position)

See it in context at: `pages/lld/design-patterns/structural/flyweight.html`
