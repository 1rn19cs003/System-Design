# Decorator — Diagrams

Rendered SVG diagram used on the website page:
- `assets/decorator/class-diagram.svg` — Coffee interface implemented by Espresso (base) and by CoffeeDecorator subclasses (Milk, Caramel) that wrap another Coffee

See it in context at: `pages/lld/design-patterns/structural/decorator.html`
