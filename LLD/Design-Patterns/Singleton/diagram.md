# Singleton — Diagrams

The rendered diagrams live as SVG images (used directly in the website page) at:
- `assets/singleton/class-diagram.svg` — class diagram (private constructor + static accessor)
- `assets/singleton/sequence-diagram.svg` — sequence diagram (two clients, same instance returned both times)

See them rendered in context on the pattern's page: `pages/lld/design-patterns/creational/singleton.html`
