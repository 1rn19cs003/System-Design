# Visitor — Diagrams

Rendered SVG diagram used on the website page:
- `assets/visitor/class-diagram.svg` — Book and Electronic implementing ItemElement.accept(visitor), calling back into PricingVisitor's visit(Book)/visit(Electronic) overloads (double dispatch)

See it in context at: `pages/lld/design-patterns/behavioral/visitor.html`
