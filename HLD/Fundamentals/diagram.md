# Fundamentals — Diagrams

Rendered SVG diagram used on the website page:
- `assets/hld-fundamentals/system-diagram.svg` — Client → DNS resolver (name → IP) → TCP connection → Server, with the request/response cycle and where latency accumulates at each hop

This is a system/architecture diagram (boxes = components, arrows = network calls), not a UML class diagram — HLD topics describe how components across a network interact, not how a single class is shaped internally.

See it in context at: `pages/hld/fundamentals.html`
